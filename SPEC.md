# 太田の習慣コーチ 仕様書

## 1. このアプリの目的

太田の習慣コーチは、筋トレ・学習・睡眠を毎日続けるための習慣化支援アプリである。

重視することは、機能を増やすことではなく、毎日の記録を見やすく、使いやすく、続けやすくすることである。

このアプリでは、完璧にできた日だけでなく、少しだけできた日や、できなかった日も記録として残す。記録を残すことで、講師がLINEで状況を確認し、本人が翌日また戻ってきやすい状態を作る。

## 2. 基本方針

### 2.1 見やすさを優先する

- 最初に見る画面は、今日の記録に集中できる構成にする
- 情報を詰め込みすぎず、必要なものから順番に表示する
- 筋トレ・学習・睡眠の状態が色で分かるようにする
- 記録済み、保留、エラーなどの状態を色付きで分かりやすく表示する

### 2.2 使いやすさを優先する

- 記録ボタンまでの距離を短くする
- 入力項目を増やしすぎない
- 画面を開いたらすぐ今日の記録を書けるようにする
- スマホでも押しやすく、読みにくくならないようにする

### 2.3 続けやすさを優先する

- 「できた」だけでなく「少しだけ」も継続として扱う
- できなかった日を責める表示にしない
- カレンダーから過去の記録を確認・編集できるようにする
- 講師へのLINE報告で、毎日の確認が自然に続くようにする

## 3. 利用者

### 3.1 本人

太田さん本人が、筋トレ・学習・睡眠の記録を行う。

主な操作:

- 今日の記録を書く
- 過去の記録を確認する
- 今日の目安を必要に応じて修正する
- LINE連携の状態を確認する

### 3.2 講師

講師は、本人が送信した日次レポートをLINEで確認する。

現段階では、講師が細かくアプリを操作することよりも、LINEで毎日の状況を確認できることを優先する。

## 4. 現在の画面構成

### 4.1 今日の記録

アプリの中心となる画面。

筋トレ・学習・睡眠の3つを記録できる。各カードは開閉でき、必要な項目だけを見ながら入力できる。

記録項目:

- 今日の目安
- 今日の結果
- 気分
- 学習時間帯と学習時間（学習のみ）
- ひとことメモ

記録後は、保存状態とLINE送信状態を画面上で確認できる。

### 4.2 今日の目安の編集

各習慣カードの編集ボタンから、今日の目安を修正できる。

例:

- 筋トレ: スクワット1回
- 学習: 教材を1分開く
- 睡眠: 布団に入る時間を決める

今日の目安は、記録と一緒に保存される。

### 4.3 記録カレンダー

日付を押すと、カレンダーを開いて過去の記録を確認できる。

できること:

- 過去日の記録を見る
- 記録済みの日を確認する
- 過去日の記録を編集する
- 記録の日付を変更する
- 不要な記録を削除する

### 4.4 LINE連携パネル

本人コード、講師コード、連携状態、Webhook URLを確認できる。

記録を保存した時に、LINE設定と講師連携が完了していれば、講師へ日次レポートを送る。

### 4.5 記録後のひとこと

記録後に、小さくコーチメッセージを表示する。

このメッセージは、画面上部を占有せず、記録後の補助として表示する。

## 5. 記録の流れ

1. アプリを開く
2. 今日の日付と記録カードを確認する
3. 筋トレ・学習・睡眠のカードを開く
4. 今日の結果、気分、メモなどを入力する
5. 必要なら今日の目安を編集する
6. 必要に応じて「送信前に確認」でLINE文面を確認する
7. 「保存してLINE送信」を押す
8. 画面に保存状態と送信状態が表示される
9. LINE連携済みの場合、本人と講師へ日次レポートが送信される

## 6. LINE連携仕様

### 6.1 目的

LINE連携は、講師が本人の習慣記録を確認するために使う。

主目的は、細かい管理ではなく、毎日の報告を講師が見られる状態にすることである。

### 6.2 連携方法

本人はLINE公式アカウントに以下を送る。

```text
連携 OTA-XXXX
```

講師はLINE公式アカウントに以下を送る。

```text
講師連携 COACH-XXXX
```

### 6.3 日次レポート

記録保存時に、講師へ日次レポートを送信する。

送信内容:

- 記録日
- 今日の状況
- 要フォローの有無
- 学習合計
- 習慣ごとの結果
- 今日の目安
- 学習時間帯
- 気分
- メモ

例:

```text
【太田さんの習慣レポート】
記録日: 2026/6/19(金)
今日の状況: できた 2 / 少し 1 / 難しい 0
要フォロー: なし
学習合計: 30分

■ 筋トレ
結果: できた
最低ライン: スクワット1回
気分: 良い
メモ: 少しだけでもできた
```

## 7. データ保存仕様

### 7.1 ブラウザ保存

画面側の記録は、ブラウザのLocalStorageに保存する。

キー:

```text
otaHabitCoach:v2
```

保存対象:

- 習慣設定
- 今日の目安
- 日次記録
- 画面設定

### 7.2 サーバー保存

LINE連携用のバックエンドは、日次記録と連携情報を保存する。

保存先:

```text
data/habit-coach-db.json
```

保存対象:

- 本人連携コード
- 講師連携コード
- LINE userId
- 日次記録

### 7.3 Render Free利用時の注意

Render Freeでは、サーバー内の保存ファイルが消える可能性がある。

そのため、LINE連携コードや講師のLINE userIdは、Renderの環境変数でも管理できるようにしている。

長期的に記録を安定保存する場合は、外部データベースや有料の保存先を検討する。

## 8. API仕様

### 8.1 `GET /api/health`

サーバーの状態、LINE設定状態、Webhook URLを返す。

### 8.2 `GET /api/link-codes`

本人コード、講師コード、連携状態を返す。

### 8.3 `GET /api/records`

サーバーに保存された日次記録を返す。

### 8.4 `POST /api/records`

日次記録を保存し、講師へのLINEレポート送信を試す。

### 8.5 `POST /api/records/bulk`

複数の習慣記録をまとめて保存し、指定日のLINEレポート送信を試す。

### 8.6 `GET /api/reports/daily/preview`

指定日のLINEレポート文面をプレビューする。

### 8.7 `POST /api/reports/daily`

指定日のLINEレポートを再送する。

### 8.8 `POST /api/line/webhook`

LINE Messaging APIのWebhookを受け取り、本人・講師の連携メッセージを処理する。

## 9. デプロイ仕様

### 9.1 ローカル起動

```sh
npm start
```

ローカルでは以下で開く。

```text
http://localhost:8001/
```

### 9.2 Render公開

外出先でスマホだけで使うため、Render Freeへの公開を想定する。

必要な環境変数:

- `LINE_CHANNEL_SECRET`
- `LINE_CHANNEL_ACCESS_TOKEN`
- `APP_BASE_URL`
- `STUDENT_LINK_CODE`
- `COACH_LINK_CODE`
- `COACH_LINE_USER_ID`

LINE DevelopersのWebhook URL:

```text
https://RenderのURL/api/line/webhook
```

## 10. 非機能要件

### 10.1 操作性

- 毎日の記録が短時間で完了すること
- スマホ幅でも入力しやすいこと
- ボタンや入力欄が小さすぎないこと
- 画面上の情報量を増やしすぎないこと
- 記録状態が色で分かること

### 10.2 安全性

- LINEのチャンネルシークレットとアクセストークンは `.env` またはRender環境変数で管理する
- 秘密情報を画面や仕様書に書かない
- `.env` と `data/` はGitHubにアップロードしない
- LINE Webhookは署名検証する

### 10.3 継続性

- 未達を責める表現にしない
- できなかった日でも次の日に戻りやすくする
- 完璧な達成より、記録を残すことを優先する

## 11. 現在の制約

- LINE連携は、本人1人・講師1人を基本とする
- 認証・ログイン機能は未実装
- 音声入力はブラウザの対応状況に依存する
- Render Freeでは、アクセスがないと一時停止する
- Render Freeでは、保存ファイルが永続化されない可能性がある
- 長期的な複数端末同期には、外部データベースの検討が必要

## 12. 今後の改善候補

優先度が高いもの:

- GitHubにアップロードする
- Renderに接続して公開URLを作る
- LINE DevelopersのWebhook URLをRender URLに変更する
- スマホから記録とLINE報告を確認する
- 仕様書とREADMEを実装内容に合わせて更新し続ける

必要になったら検討するもの:

- 外部データベースへの移行
- 講師用の閲覧画面の強化
- 週次・月次レポート
- LINEからの記録入力
- 通知・リマインダー
- アカウント登録
- 複数講師対応

## 13. MVP完了条件

現段階のMVP完了条件は以下とする。

- 筋トレ・学習・睡眠を記録できる
- 今日の目安を編集できる
- 記録フォームがシンプルで入力しやすい
- カードを開閉して必要な項目だけ確認できる
- 過去日の記録をカレンダーから確認・編集できる
- 保存状態とLINE送信状態が画面で分かる
- LINEで本人・講師を連携できる
- 記録保存時に講師へ日次レポートを送信できる
- Renderで公開し、スマホから外出先でも開ける
